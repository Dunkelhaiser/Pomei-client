import Loader from "../../components/Loader/Loader";

const Loading = () => {
    return (
        <section className="centered">
            <Loader />
        </section>
    );
};
export default Loading;
